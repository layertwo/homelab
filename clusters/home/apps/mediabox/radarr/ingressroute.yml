---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr
  namespace: mediabox
  annotations:
    external-dns.alpha.kubernetes.io/enabled: "true"
    external-dns.alpha.kubernetes.io/hostname: "radarr.layertwo.dev"
    external-dns.alpha.kubernetes.io/target: "proxy-external.layertwo.dev"
    layertwo.dev/publish: "all"
    cert-manager.io/cluster-issuer: letsencrypt-prod-dns
    kubernetes.io/ingress.class: external
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`radarr.layertwo.dev`) && PathPrefix(`/ping`)
    kind: Rule
    services: &services
    - name: radarr
      port: 7878
    middlewares:
      - name: default-headers-mediabox
  - match: Host(`radarr.layertwo.dev`)
    kind: Rule
    services: *services
    middlewares:
      - name: default-headers-mediabox
      - name: mediabox-oidc
