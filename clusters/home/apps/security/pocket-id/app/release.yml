---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pocket-id
  namespace: pocket-id
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 5.2.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          pocket-id:
            image:
              repository: ghcr.io/pocket-id/pocket-id
              tag: v2.14.0
            env:
              APP_URL: "https://idp.layertwo.dev"
              APP_NAME: "layertwo"
              ANALYTICS_DISABLED: "true"
              ALLOW_USER_SIGNUPS: "withToken"
              TRUST_PROXY: "true"
              FILE_BACKEND: "database"
              SMTP_PORT: "465"
              SMTP_FROM: "noreply@idp.layertwo.dev"
              SMTP_TLS: "tls"
              EMAIL_LOGIN_NOTIFICATION_ENABLED: "false"
              EMAIL_ONE_TIME_ACCESS_AS_ADMIN_ENABLED: "true"
              EMAIL_ONE_TIME_ACCESS_AS_UNAUTHENTICATED_ENABLED: "false"
              EMAIL_API_KEY_EXPIRATION_ENABLED: "false"
              DB_CONNECTION_STRING:
                secretKeyRef:
                  name: cnpg-pocket-id-app
                  key: uri
            envFrom:
              - secretRef:
                  name: secrets-pocket-id
              - secretRef:
                  name: secrets-smtp
            securityContext:
              allowPrivilegeEscalation: false
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 1Gi
            probes:
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 1411
                  initialDelaySeconds: 5
                  periodSeconds: 10
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 1411
                  periodSeconds: 5
                  failureThreshold: 60
    service:
      main:
        enabled: true
        controller: main
        ports:
          http:
            enabled: true
            port: 1411
            protocol: HTTP
