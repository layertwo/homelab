# PLAINTEXT — encrypt with sops before committing.
# Same values as immich/postgres/secrets-cloudnativepg-r2.sops.yml.
apiVersion: v1
kind: Secret
metadata:
  name: secrets-cloudnativepg-r2
  namespace: vaultwarden
type: Opaque
stringData:
  ACCESS_KEY_ID: REPLACE_ME
  SECRET_ACCESS_KEY: REPLACE_ME
