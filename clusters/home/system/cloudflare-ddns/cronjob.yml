---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cloudflare-ddns
  namespace: traefik-system
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Replace
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 3
      template:
        spec:
          containers:
          - name: main
            imagePullPolicy: IfNotPresent
            securityContext:
              runAsNonRoot: false
              readOnlyRootFilesystem: true
              #capabilities:
                #drop:
                #  - ALL
              privileged: false
              allowPrivilegeEscalation: false
            env:
              - name: TOKEN
                valueFrom:
                  secretKeyRef:
                    key: TOKEN
                    name: cloudflare-ddns
                    optional: false
              - name: ZONE_ID
                valueFrom:
                  secretKeyRef:
                    key: ZONE_ID
                    name: cloudflare-ddns
                    optional: false
              - name: DNS_NAME
                value: "proxy-external.layertwo.dev"
            image: ghcr.io/layertwo/cloudflare-ddns:latest@sha256:c1aa883700f24e4230327aa6c3633bb239373a829b9b5975eae692b7b375ca7f
          restartPolicy: OnFailure
          dnsConfig:
            nameservers:
              - 1.1.1.1
